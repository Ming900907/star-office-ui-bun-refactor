# Upstream mirror (reference only, do not publish in this repo)
upstream/

# Runtime state files (local/private)
state.json
agents-state.json
join-keys.json
asset-positions.json
asset-defaults.json
runtime-config.json

# Runtime storage
storage/**
!storage/.gitkeep
assets/home-favorites/**
!assets/home-favorites/.gitkeep

# Runtime backup files generated by asset replacement flows
frontend/*.bak
frontend/*.bak.*
frontend/*.default
frontend/*.tmp

# Dependencies / build artifacts
node_modules/
electron-shell/node_modules/
dist/
build/

# OS/editor noise
.DS_Store
*.log
